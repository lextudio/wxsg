<UserControl x:Class="SimpleSampleCrashRepro.FakeOutline"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SimpleSampleCrashRepro">
    <DockPanel LastChildFill="True">
        <TextBox DockPanel.Dock="Top"
                 Height="20"
                 Text="{Binding Filter, ElementName=OutlineTreeView}" />
        <local:FakeOutlineTree x:Name="OutlineTreeView"
                               Root="{Binding Root, ElementName=root}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </local:FakeOutlineTree>
    </DockPanel>
</UserControl>
