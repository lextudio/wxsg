<Window x:Class="TemplateNameScopeSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Template NameScope Sample"
        Width="320"
        Height="220">
    <Window.Resources>
        <DataTemplate x:Key="StopTemplate">
            <Grid>
                <Path x:Name="thumb"
                      Data="M 0,0 L 4,0 4,4 0,4 Z"
                      Fill="Orange" />
            </Grid>
            <DataTemplate.Triggers>
                <Trigger Property="Selector.IsSelected"
                         Value="True">
                    <Setter TargetName="thumb"
                            Property="Fill"
                            Value="Gold" />
                </Trigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <Style x:Key="TemplatedButtonStyle"
               TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid>
                            <Path x:Name="thumb"
                                  Data="M 0,0 L 4,0 4,4 0,4 Z"
                                  Fill="White" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="thumb"
                                        Property="Fill"
                                        Value="Red" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="16">
        <ContentPresenter ContentTemplate="{StaticResource StopTemplate}"
                          Content="Sample" />
        <Button Margin="0,12,0,0"
                Style="{StaticResource TemplatedButtonStyle}"
                Content="Click" />
    </StackPanel>
</Window>
